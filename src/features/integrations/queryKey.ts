/** TanStack Query keys for the Sunday-suite integrations panel (shared by the
 *  panel and any caller that needs to invalidate them). */
export const INTEGRATIONS_SETTINGS_KEY = ["integrations", "settings"] as const;
export const BRIDGE_STATUS_KEY = ["integrations", "bridgeStatus"] as const;
