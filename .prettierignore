# Build output & deps
dist
node_modules
src-tauri/target
src-tauri/gen
src-tauri/binaries

# Generated by ts-rs (cargo test export_bindings) — formatting these would just
# drift on every regeneration; the bindings drift-gate in CI guards their shape.
src/lib/bindings
