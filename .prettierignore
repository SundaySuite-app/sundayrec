# Build output & deps
dist
node_modules
src-tauri/target
src-tauri/gen
src-tauri/binaries
target

# The ported old Electron renderer is a VERBATIM copy — reformatting it would
# break the byte-for-byte parity with the old app. Leave it exactly as shipped.
legacy

# Kept React reference (hooks) — not part of the build.
reference

# ts-rs bindings: src/lib/bindings is the transient regen target; the committed
# copy lives at legacy/bindings (already covered by the `legacy` entry above —
# generated code is never reformatted).
src/lib/bindings
