# Build output & deps
dist
node_modules
src-tauri/target
src-tauri/gen
src-tauri/binaries
target

# `legacy/` is what the inventory left behind: the ts-rs `bindings/` (generated
# code is never reformatted), the seven locale catalogues, `types/` and
# `shared/` — all verbatim port or generated output, same reasoning as above.
legacy

# ts-rs bindings: src/lib/bindings is the transient regen target; the committed
# copy lives at legacy/bindings (already covered by the `legacy` entry above).
src/lib/bindings

# The atlas: PNGs plus two markdown reports that `npm run atlas` REGENERATES on
# every run (e2e/atlas/report.ts). Format-gating generated output would mean
# every photo session leaves the tree dirty. `atlas-v015/` is the fase A archive
# — the same generated output, frozen. docs/design/ATLAS.md is hand-written and
# stays covered.
docs/design/atlas
docs/design/atlas-v015
